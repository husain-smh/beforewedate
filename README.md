
  # before we date

  This is a code bundle for before we date. The original project is available at https://www.figma.com/design/OAQLEFYhie1bddITyhsLps/Know-That-Person-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  